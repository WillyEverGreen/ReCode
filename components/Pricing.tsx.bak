import React from "react";
import {
  Check,
  X,
  Sparkles,
  Zap,
  Crown,
  ArrowRight,
  Shield,
  Infinity,
} from "lucide-react";

interface PricingProps {
  onGetStarted: () => void;
}

const Pricing: React.FC<PricingProps> = ({ onGetStarted }) => {
  const plans = [
    {
      name: "Free",
      price: "$0",
      period: "forever",
      description: "Perfect for getting started with ReCode",
      icon: <Sparkles className="w-6 h-6" />,
      color: "blue",
      features: [
        { text: "2 Get Solution requests/day", included: true },
        { text: "3 Add Solution (code analysis)/day", included: true },
        { text: "1 Custom variant/day", included: true },
        { text: "All programming languages", included: true },
        { text: "Pattern recognition", included: true },
        { text: "Complexity analysis", included: true },
        { text: "Auto-generated notes", included: true },
        { text: "Dashboard & search", included: true },
        { text: "Priority support", included: false },
        { text: "Advanced analytics", included: false },
        { text: "Custom export formats", included: false },
      ],
      buttonText: "Get Started",
      popular: false,
    },
    {
      name: "Pro",
      price: "$9",
      period: "/month",
      description: "For serious interview preparation",
      icon: <Crown className="w-6 h-6" />,
      color: "yellow",
      features: [
        { text: "Unlimited Get Solution", included: true },
        { text: "Unlimited Add Solution", included: true },
        { text: "Unlimited custom variants", included: true },
        { text: "All programming languages", included: true },
        { text: "Pattern recognition", included: true },
        { text: "Complexity analysis", included: true },
        { text: "Auto-generated notes", included: true },
        { text: "Dashboard & search", included: true },
        { text: "Priority support", included: true },
        { text: "Advanced analytics", included: true },
        { text: "Custom export formats", included: true },
        { text: "Early access to new features", included: true },
      ],
      buttonText: "Upgrade to Pro",
      popular: true,
    },
  ];

  return (
    <div className="min-h-screen bg-[#0b0f19] text-gray-100 py-16 px-4">
      {/* Header */}
      <div className="max-w-7xl mx-auto text-center mb-16">
        <div className="inline-flex items-center gap-2 px-4 py-2 bg-yellow-500/10 border border-yellow-500/20 rounded-full text-yellow-400 text-sm font-medium mb-6">
          <Zap className="w-4 h-4" />
          Simple, Transparent Pricing
        </div>

        <h1 className="text-5xl sm:text-6xl font-bold mb-6 leading-tight">
          Choose Your{" "}
          <span className="text-transparent bg-clip-text bg-gradient-to-r from-yellow-500 to-cyan-400">
            Interview Prep
          </span>{" "}
          Plan
        </h1>

        <p className="text-xl text-gray-400 max-w-2xl mx-auto leading-relaxed">
          Start free and upgrade anytime. All plans include core features to help
          you master DSA and ace your interviews.
        </p>
      </div>

      {/* Pricing Cards */}
      <div className="max-w-6xl mx-auto grid md:grid-cols-2 gap-8 mb-16">
        {plans.map((plan, idx) => (
          <div
            key={idx}
            className={`relative rounded-2xl p-8 transition-all duration-300 ${
              plan.popular
                ? "bg-gradient-to-b from-yellow-500/10 to-transparent border-2 border-yellow-500/30 shadow-2xl shadow-yellow-900/20 scale-105"
                : "bg-gray-900/40 border border-gray-800 hover:border-gray-700"
            }`}
          >
            {/* Popular Badge */}
            {plan.popular && (
              <div className="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 bg-gradient-to-r from-yellow-500 to-orange-500 rounded-full text-white text-sm font-bold shadow-lg">
                ⭐ Most Popular
              </div>
            )}

            {/* Plan Header */}
            <div className="mb-8">
              <div
                className={`inline-flex p-3 rounded-xl mb-4 ${
                  plan.color === "yellow"
                    ? "bg-yellow-500/10"
                    : "bg-blue-500/10"
                }`}
              >
                {plan.icon}
              </div>

              <h3 className="text-2xl font-bold text-white mb-2">{plan.name}</h3>
              <p className="text-gray-400 text-sm mb-6">{plan.description}</p>

              <div className="flex items-baseline gap-2 mb-2">
                <span className="text-5xl font-bold text-white">{plan.price}</span>
                <span className="text-gray-500 text-lg">{plan.period}</span>
              </div>

              {plan.name === "Free" && (
                <p className="text-xs text-gray-500">No credit card required</p>
              )}
            </div>

            {/* Features List */}
            <ul className="space-y-4 mb-8">
              {plan.features.map((feature, featureIdx) => (
                <li key={featureIdx} className="flex items-start gap-3">
                  {feature.included ? (
                    <Check className="w-5 h-5 text-green-400 flex-shrink-0 mt-0.5" />
                  ) : (
                    <X className="w-5 h-5 text-gray-600 flex-shrink-0 mt-0.5" />
                  )}
                  <span
                    className={`text-sm ${
                      feature.included ? "text-gray-300" : "text-gray-600"
                    }`}
                  >
                    {feature.text}
                  </span>
                </li>
              ))}
            </ul>

            {/* CTA Button */}
            <button
              onClick={onGetStarted}
              className={`w-full py-4 px-6 rounded-xl font-bold text-lg transition-all duration-300 flex items-center justify-center gap-2 group ${
                plan.popular
                  ? "bg-yellow-600 hover:bg-yellow-500 text-white shadow-lg shadow-yellow-900/20 hover:shadow-yellow-900/40"
                  : "bg-gray-800 hover:bg-gray-700 text-white"
              }`}
            >
              {plan.buttonText}
              <ArrowRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
            </button>
          </div>
        ))}
      </div>

      {/* Trust Signals */}
      <div className="max-w-5xl mx-auto grid md:grid-cols-3 gap-6 mb-16">
        <div className="p-6 bg-gray-900/30 rounded-xl border border-gray-800 text-center">
          <Shield className="w-8 h-8 text-green-400 mx-auto mb-3" />
          <h4 className="font-bold text-white mb-2">Secure & Private</h4>
          <p className="text-sm text-gray-400">
            Your code and data are encrypted and never shared
          </p>
        </div>

        <div className="p-6 bg-gray-900/30 rounded-xl border border-gray-800 text-center">
          <Zap className="w-8 h-8 text-yellow-400 mx-auto mb-3" />
          <h4 className="font-bold text-white mb-2">Lightning Fast</h4>
          <p className="text-sm text-gray-400">
            AI-powered analysis in seconds, not minutes
          </p>
        </div>

        <div className="p-6 bg-gray-900/30 rounded-xl border border-gray-800 text-center">
          <Infinity className="w-8 h-8 text-cyan-400 mx-auto mb-3" />
          <h4 className="font-bold text-white mb-2">Cancel Anytime</h4>
          <p className="text-sm text-gray-400">
            No lock-in. Upgrade, downgrade, or cancel anytime
          </p>
        </div>
      </div>

      {/* FAQ Section */}
      <div className="max-w-3xl mx-auto mb-16">
        <h2 className="text-3xl font-bold text-center mb-10">
          Pricing FAQs
        </h2>

        <div className="space-y-6">
          {[
            {
              q: "What happens when I hit the daily limit on Free plan?",
              a: "You'll see a friendly message letting you know you've reached your limit. Your limits reset at midnight UTC every day. You can upgrade to Pro anytime for unlimited access!",
            },
            {
              q: "Can I upgrade from Free to Pro anytime?",
              a: "Absolutely! You can upgrade to Pro plan at any time. Your existing data and progress will be preserved, and you'll immediately get unlimited access.",
            },
            {
              q: "What payment methods do you accept?",
              a: "We accept all major credit cards (Visa, Mastercard, American Express) through our secure payment processor Stripe.",
            },
            {
              q: "Is there a student discount?",
              a: "Yes! Students can get 50% off Pro plan. Contact us with your .edu email for a discount code.",
            },
          ].map((faq, idx) => (
            <div
              key={idx}
              className="p-6 bg-gray-900/30 rounded-xl border border-gray-800"
            >
              <h4 className="font-bold text-white mb-3">{faq.q}</h4>
              <p className="text-gray-400 text-sm leading-relaxed">{faq.a}</p>
            </div>
          ))}
        </div>
      </div>

      {/* Bottom CTA */}
      <div className="max-w-4xl mx-auto text-center p-12 bg-gradient-to-r from-yellow-500/10 via-blue-500/10 to-cyan-500/10 rounded-2xl border border-gray-800">
        <h2 className="text-4xl font-bold mb-4">
          Still have questions?
        </h2>
        <p className="text-gray-400 mb-6 text-lg">
          We're here to help! Reach out to us at{" "}
          <a
            href="mailto:support@recode.app"
            className="text-yellow-400 hover:text-yellow-300 transition-colors"
          >
            support@recode.app
          </a>
        </p>
        <button
          onClick={() => {
            // Future: Open chat/support
          }}
          className="px-8 py-3 bg-yellow-600 hover:bg-yellow-500 rounded-xl font-bold transition-all shadow-lg shadow-yellow-900/20 hover:shadow-yellow-900/40 text-white"
        >
          Contact Support
        </button>
      </div>
    </div>
  );
};

export default Pricing;
